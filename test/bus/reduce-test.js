var mesh = require('../..');

var ReduceBus = mesh.ReduceBus;
var Bus = mesh.Bus;

var expect = require('expect.js');
var co = require('co');

describe(__filename + '#', function() {

  // xit('is a bus', function() {
  //   expect(new ReduceBus()).to.be.an(Bus);
  // });
  //
  // xit('can reduce many chunks into a single one', co.wrap(function*() {
  // }));


});
