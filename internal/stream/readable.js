var extend = require('../extend');

/**
 */

function ReadableStream(chunkQueue) {
  // if (!underlyingSource) underlyingSource = {};

}

/**
 */

extend(ReadableStream, {

  /**
   */

  pipeTo: function(writable, options) {
    if (!options) options = {};
  }
});

/**
 */

module.exports = ReadableStream;
