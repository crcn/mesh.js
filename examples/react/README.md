Simple todo list demonstration of how you can use `mesh` with ReactJS.

#### Features

- Uses local storage. Easily swap data source out for something else.
- Similar flux philosophy. Data is loaded only in the root component. 
